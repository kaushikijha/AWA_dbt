version: 2

sources:
  - name: raw_src  # Source system name
    database: raw_dbt      # Optional: database where source is stored
    schema: raw_Schema  # Schema containing the source tables
    tables:
      - name: raw_customer 
        # Table name as it exists in the database
      - name: raw_order

      - name: raw_orderitem

      - name: raw_payment
      
      - name: raw_product

